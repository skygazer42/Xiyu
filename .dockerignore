# Python caches
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage

# Node / frontend
frontend/node_modules/
frontend/.next/
frontend/dist/

# Local runtime data (mounted at runtime; do not send into docker build context)
data/models/
data/uploads/
data/outputs/
data/_tmp/

# Local media samples (keep in repo for smoke tests, but don't send to docker build context)
*.m4a
*.mp3
*.wav
*.flac
*.ogg
*.mp4
*.avi
*.mkv

# Optional: local clone of VibeVoice (mounted into vLLM container, not needed for builds)
VibeVoice/
third_party/VibeVoice/

# Git
.git/
