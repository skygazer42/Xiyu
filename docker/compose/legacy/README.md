# Legacy Compose Files

This directory contains legacy / advanced Docker Compose variants that were previously kept at the repo root.

As of the “single entry” deployment simplification, the **recommended** deployment path is:

- Repo root `docker-compose.yml` (single-stack, public entry: `xiyu-router`)

These legacy files are kept for reference / experiments, but are no longer the primary supported path and may drift.

